schema {
  query: Query
}

type BoardGameResult {
  id: Int!
  maxPlayers: Int
  minPlayers: Int
  name: String!
  playtime: Int
  year: Int
}

type Query {
  games(username: String!): [BoardGameResult!]!
}